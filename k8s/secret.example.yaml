# Template only — do not put real secrets in this file.
# deploy.ps1 creates the real Secret directly in the cluster via `kubectl create
# secret`, reading values from your repo-root .env — nothing is written to disk.
apiVersion: v1
kind: Secret
metadata:
  name: personal-assistant-secrets
  namespace: personal-assistant
type: Opaque
stringData:
  SA_PASSWORD: "CHANGE_ME_Strong@Password123"
  JWT_KEY: "change-me-32-characters-minimum-secret-key!!"
  CONNECTION_STRING: "Server=sql,1433;Database=BudgetApp;User Id=sa;Password=CHANGE_ME_Strong@Password123;Encrypt=False;TrustServerCertificate=True;"
